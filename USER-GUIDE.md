# PMFlow 使用手册

这份文档给你自己看。目标是让你打开 Claude Code 后，不需要回忆这段聊天，也能把 DeepSeek 带着往下做。

## 1. 当前项目在哪里

PMFlow 项目目录：

```text
D:\work\PMFlow
```

旧 OMP 只作为参考资料：

```text
D:\work\OhMyPm
```

testany-eng 参考资料：

```text
D:\work\refs\testany-agent-skills\plugins\testany-eng
```

## 2. 第一次打开 Claude Code 后怎么做

在 Claude Code CLI 中进入：

```text
D:\work\PMFlow
```

然后先让 DeepSeek 读取这些文件：

```text
AGENTS.md
prd.machine.yaml
handoff.md
anti-patterns.md
```

第一句话可以直接发：

```text
请先读取 AGENTS.md、prd.machine.yaml、handoff.md、anti-patterns.md。读取后只总结你理解到的执行纪律、产品目标和禁止事项，不要改任何文件。
```

确认它理解正确后，再进入下一步。

## 3. 正式开始时怎么说

第二句话可以发：

```text
现在开始 Phase 0：现状审计。请参考 handoff.md 中提到的旧 OMP、ShitPM、testany-eng 经验，只输出审计报告，不要修改实现文件。
```

如果它问要审计什么，告诉它：

```text
先审计 PMFlow 当前 7 份交接材料是否足够支持后续设计；再审计旧 OMP 和 testany-eng 中哪些结构值得参考。输出 v0.1 设计前审计报告。
```

## 4. 你每一步要等什么

### Phase 0：审计

你应该收到：

- 当前交接材料是否完整。
- 旧 OMP 哪些经验可复用。
- testany-eng 哪些结构可参考。
- 不能继承哪些旧机制。
- 下一阶段设计要解决什么。

不能收到：

- 直接创建一堆 skill。
- 直接开始写代码。
- 直接说“我已完成整体重构”。

### Phase 1：设计方案

你应该收到：

- 新项目目录结构。
- skill 清单。
- 命令清单。
- 状态文件设计。
- metadata/profile/schema 设计。
- writer/reviewer/stop 如何落地。

你需要重点看：

- 是否每阶段都停。
- 是否保留 PM 确认。
- 是否避免自然语言自动路由。
- 是否过度设计。

### Phase 2：先做 guide

你应该收到：

- `pm-guide` 的命令和 skill 设计。
- 新项目第一次启动时怎么避免误读项目文件。
- 当前阶段判断逻辑。

你要重点检查：

- 没有状态时，它不能自动生成产物。
- 有状态时，它只推荐唯一下一步。

### Phase 3：先做 brd / uc

你应该收到：

- `brd-interviewer`
- `uc-interviewer`
- brd / uc 的确认机制
- 原始需求、背景材料、会后回答的区分规则
- 用户路径和任务流的输出结构

你要重点检查：

- 背景材料不会被当作会后回答。
- interviewer 不会为了完整感直接生成方案。
- brd / uc 阶段结束后会停下等待你确认。

### Phase 4：再做 solution 链

你应该收到：

- `solution-writer`
- `solution-reviewer`
- solution profile
- solution metadata
- solution template

你要重点检查：

- 字段有没有提前出现。
- 列表字段有没有信息优先级。
- 审批/提交有没有流程口径。
- 小改动有没有 diff。

### Phase 5：真实项目回归

优先用：

```text
D:\work\交投软件中心\审计系统
```

你要重点看：

- 审计系统被判为新建，而不是混合。
- 年度审计计划列表写了关键字段。
- 年度审计计划详情/表单写了字段或字段分组。
- 提交审批有流程。

## 5. 什么时候要立刻叫停 DeepSeek

出现下面情况，直接停：

```text
停止。你正在自动跨阶段。请回到当前阶段，只输出当前阶段产物和下一步命令。
```

```text
停止。你把背景材料当成会后回答了。请重新区分原始需求、背景材料、会后回答。
```

```text
停止。你还没有审计就开始实现了。请先输出审计报告。
```

```text
停止。你把样例当成规则了。请重新按 AGENTS.md 的分层规则处理。
```

```text
停止。这个产物模板感太强。请改成更像人类 PM 写的结构化自然语言。
```

```text
停止。不要整篇重写。请先列影响范围，再给局部 diff。
```

## 6. 每个阶段你应该要求它怎么交付

每个阶段结束，你都可以要求：

```text
请按以下格式交付：

已完成：
- ...

修改文件：
- ...

新增文件：
- ...

未解决问题：
- ...

需要我确认：
- ...

下一步唯一命令：
- ...
```

如果它没有列“需要我确认”，说明它又在往全自动方向跑。

## 7. 怎么交给 Codex 验收

当 DeepSeek 完成一个阶段后，切到 Codex，进入：

```text
D:\work\PMFlow
```

然后说：

```text
读取 review-manual.md 和 codex-memory-handoff.md，验收 DeepSeek 刚完成的 PMFlow 阶段。重点看是否违反 PM ownership gate、writer/reviewer 分离、人读/机读分离。
```

如果是 solution 链，补一句：

```text
重点用审计系统案例检查建设类型、字段、流程、列表信息架构和小改动 diff。
```

## 8. 常用提示词

### 让 DeepSeek 继续下一阶段

```text
我已看过并确认当前阶段产物。请进入下一阶段，但只做下一阶段，不要继续往后推进。
```

### 要求它先给方案，不改文件

```text
先只给设计方案，不要改文件。说明你准备改哪些文件、为什么改、验收标准是什么。
```

### 要求它局部修改

```text
不要整篇重写。请先列影响范围，再只修改必要位置，并给我变更摘要。
```

### 要求它说明为什么停下

```text
请说明当前阶段为什么必须停下，以及我需要看懂/确认什么才能进入下一阶段。
```

### 要求它准备给 Codex 验收

```text
请整理本阶段交付清单、修改文件、未解决问题和需要 Codex 验收的点。
```

## 9. 你要记住的判断标准

PMFlow 好不好，不看它一次性能生成多少东西。

看这些：

- 你是否知道它做到哪一步。
- 你是否看过并理解当前产物。
- 它是否等你确认再继续。
- 它是否提前暴露字段、流程、页面问题。
- 它是否能局部修正。
- 它是否让你在评审会上不用重新翻半天文档。

如果这些做不到，就算文档生成得很完整，也不是合格的 PMFlow。
