# PMFlow 反模式清单

本文件列出 PMFlow 项目中必须避免的坏味道。执行者看到这些情况应立即停下修正。

## P0：必须立即阻断

### 1. 自动跨阶段

坏表现：

- 用户确认了一个阶段，AI 直接开始下一阶段。
- solution 写完后直接写 prototype。
- prototype 写完后直接写 PRD。

正确做法：

- 只提示下一步命令。
- 等 PM 显式执行下一命令。

### 2. 背景材料冒充会后回答

坏表现：

- 用户补充“项目背景.md”，AI 当成第一轮回答。
- 材料和问题无关，AI 也强行回填。

正确做法：

- 背景材料只补事实。
- 会后回答必须能对应具体问题。

### 3. reviewer 复述 writer

坏表现：

- reviewer 只说“整体完整，可以进入下一步”。
- reviewer 不指出缺字段、缺流程、缺页面、缺规则。

正确做法：

- reviewer 按 profile 检查。
- reviewer 必须能阻断。

### 4. 机读信息进入人读产物

坏表现：

- `anchor_id`
- `rules_ref`
- `prototype_ref`
- `M02-P04-A03`
- 本地绝对路径
- 调试字段

正确做法：

- 人读产物只出现自然页面名、短编号和业务说明。
- 机读信息放 `.pmflow/metadata/`。

### 5. 小改动整篇重写

坏表现：

- 用户改一个字段，AI 输出整篇新 PRD。
- 用户补一句流程，AI 重写整份 solution。

正确做法：

- 先列影响范围。
- 再给局部 diff 或变更摘要。
- 只改必要文件。

## P1：必须修正

### 6. 把样例当规则

坏表现：

- `references/` 里的例子变成必须照抄的模板。
- 好坏样例被当成门禁规则。

正确做法：

- 核心规则放 `skills/` 或 `contracts/`。
- 样例只帮助理解。

### 7. 把纠错案例写成规则

坏表现：

- 因为某次写错了 Drawer，就写“不得写 Drawer”。
- 因为某次出现 AI 痕迹，就写一堆“不得出现某句话”。

正确做法：

- 抽象成目标状态。
- 只把门禁、安全、防幻觉写成禁止式。

### 8. skill 变成说明书

坏表现：

- `SKILL.md` 很长。
- 里面有背景故事、示例、解释、一次性讨论结论。

正确做法：

- `SKILL.md` 只放 SOP。
- 示例和说明放 `references/`。

### 9. template 代替 skill

坏表现：

- AI 直接按模板填空，不读 skill。
- 模板里写流程判断、门禁、路由。

正确做法：

- 模板只管骨架。
- skill 管动作。
- contract 管跨阶段约束。

### 10. 状态文件过度聪明

坏表现：

- status 里塞满推理过程。
- status 变成另一个 PRD。

正确做法：

- status 只记录阶段、路径、确认、问题、结果。
- 复杂追溯放 metadata。

## P2：质量下降信号

### 11. 方案稿填空感强

坏表现：

- 每个页面都机械写同样小标题。
- 简单页面也硬塞字段、动作、约束。

正确做法：

- 简单页轻写。
- 复杂页重写。
- 只在有价值处展开。

### 12. PRD 依赖原型才能读懂

坏表现：

- “详见原型”
- “原型已说明”
- “本章只保留边界”

正确做法：

- PRD 必须可独立归档。
- 原型用于理解页面，PRD 用于固化规则。

### 13. 列表页没有信息优先级

坏表现：

- 表单有几十个字段，列表随便列几个。
- 不说明默认展示字段。

正确做法：

- 按用户判断任务选择字段。
- 列表只放最影响判断和下一步动作的字段。

### 14. 流程只有按钮没有口径

坏表现：

- 写了“提交审批”，但没写审批去向。
- 写了“驳回”，但没写回到哪里。

正确做法：

- 写清入口、提交后、通过后、驳回后、状态变化。

### 15. 方法论写给用户看

坏表现：

- 产物里出现“根据 Gap Analysis”。
- 原型里写“基于信息架构分析”。

正确做法：

- 方法论用于生成前内部推导。
- 对外只呈现业务结果。

## 16. 最危险的错觉

最危险的不是 AI 不会做，而是 AI 看起来做得很完整。

PMFlow 宁可停下来问 PM，也不要自作主张补完整。
